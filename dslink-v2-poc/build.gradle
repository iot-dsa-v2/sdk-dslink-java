apply plugin: 'java'
apply plugin: 'application'

//sourceCompatibility = 1.8
//targetCompatibility = 1.8
group 'org.iot-dsa'
mainClassName = 'org.iot.dsa.dslink.DSLink'

ext.moduleName = 'org.iot.dsa.dslink.v2.poc'

applicationDefaultJvmArgs = [
        "-XX:+HeapDumpOnOutOfMemoryError", "-XX:HeapDumpPath=" + name + ".hprof"]

repositories {
    mavenLocal()
    mavenCentral()
}

dependencies {
    implementation project(':dslink-v2')
    implementation project(':dslink-v2-websocket')
}

applicationDistribution.from(new File(project.projectDir, "/dslink.json"))

run {
    args System.getProperty("exec.args", "").split()
    workingDir project.buildDir
}

